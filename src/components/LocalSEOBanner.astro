---
const { lang = "en" } = Astro.props;
const site = lang === "ur" ? "urdu/" : "";
const btn1 = lang === "ur" ? "🚀 مفت مشورہ" : "🚀 Get Free Audit";
const btn2 = lang === "ur" ? "خدمات دیکھیں" : "See Services";

const tag = lang === "ur"
  ? "مقامی SEO - پاکستان کی #1 ایجنسی"
  : "Local SEO - Pakistan’s #1 Agency";
---
